# Paste This Into Claude Code

Read `CLAUDE.md`, `charter/MASTER_CHARACTER_CHARTER.md`, `docs/VISION_AND_BOUNDARIES.md`, `docs/CURRENT_MISSION.md`, and `docs/DEVELOPMENT_PROTOCOL.md` in full.

Work directly in the repository and complete only the active mission in `docs/CURRENT_MISSION.md`.

Run the baseline tests before editing. Create the requested audit, implement the fixes, add deterministic tests, run the complete verification suite, and produce the requested handoff.

Do not answer with a plan only. Do not redesign or reduce Master Character. Do not start later workstreams. Do not run a live API call unless the mission explicitly permits it and the key already exists locally.

Begin now by inspecting the repository and running the baseline commands.
